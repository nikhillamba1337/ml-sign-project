version: '3.8'

services:
  sign-language-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_CLIENT_LOGGING_LEVEL=debug
    restart: unless-stopped
